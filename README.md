# seqkit
distroless container for seqkit

## Quick Usage

```bash
# Pull the image
docker pull docker.io/picotainers/seqkit:latest

# Run the tool
docker run --rm docker.io/picotainers/seqkit:latest seqkit --help
```
